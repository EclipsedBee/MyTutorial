BeeEditNewFile；
时间是2020/0910/1337；
今天的目标是：
1，搞懂CMake是啥；
2，搞懂VSCode是啥；
因为很多大神其实都喜欢Linux，很多开源项目都是跨平台，要用到CMake；
而且CMake是控制台操作，会帮助搞懂编译原理；
它本身是一个编译器，而不是VS这样的IDE，VS中合成了编译器；
所以VS简化了编译配置，其实不便于理解编译过程；
EditNewFileBee；
